# Pinned to the versions verified working locally and in production.
# groq must be >=1.x: older releases pass a `proxies` kwarg that httpx>=0.28
# no longer accepts, which silently breaks client init in the serverless build.
Flask==3.1.3
flask-cors==6.0.5
python-dotenv==1.2.2
groq==1.5.0
firebase-admin==7.5.0
httpx==0.28.1
